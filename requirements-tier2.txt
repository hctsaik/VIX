# VIX Tier-2 / demo 環境(FiftyOne GUI + embedding 分群 + Playwright 自檢)
# 用法:  pip install -e .            # 先裝核心(numpy/pyyaml/pillow)
#         pip install -r requirements-tier2.txt
#
# 重點:必須用 Python 3.11(FiftyOne 目前不支援 3.13/3.14)。
# 版本為本機實測 known-good(2026-06)。torch 用官方 CPU wheel(無需 GPU)。

--extra-index-url https://download.pytorch.org/whl/cpu

fiftyone==1.16.0
fiftyone-brain==0.21.6
umap-learn==0.5.12
scikit-learn==1.8.0
playwright==1.60.0
torch==2.12.0
torchvision==0.27.0
